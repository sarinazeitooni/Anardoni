import Head from 'next/head';
import Main from "../components/Main/Main";
import MainMobile from "../components/Main/MainMobile";
import React, {useState} from "react";
import useViewport from "../customHook/useDeviceDetect";
import NotMobile from "../customHook/notMobile";
export default function Home() {
    const notMobile = NotMobile();
    const [isMobile,setIsMobile] = useState(useViewport());
    return (
        <React.Fragment>
            <Head>
                <title>ANARDONY</title>
                <meta name="description" content="Generated by create next app"/>
                <link rel="icon" href="https://anardoni.com/favicon-32x32.png"/>
            </Head>
            {/*<Main/>*/}
            {/*{isMobile ? <React.Fragment><MainMobile/></React.Fragment> : <React.Fragment><Main/></React.Fragment>}*/}
            {/*{(isMobile && !notMobile) ? <MainMobile/> : <Main/> }*/}
            {isMobile ? <MainMobile/> : <Main/>}
            {/*{notMobile ? <Main/> : <MainMobile/>}*/}
        </React.Fragment>
    )
}